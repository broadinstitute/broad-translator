# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

# Translator REST API
GET     /modelList                  controllers.TranslatorController.getModelList
GET     /:model/signature           controllers.TranslatorController.getModelSignature(model)
GET     /:model/vars/:group         controllers.TranslatorController.getVariablesByGroup(model, group)
POST    /evaluate                   controllers.TranslatorController.evaluate

# smartAPI

GET     /:model/smart               controllers.TranslatorController.smart(model)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
