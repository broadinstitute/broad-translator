# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

# Translator REST API
GET     /getModelList               controllers.TranslatorController.getModelList
POST    /getModelSignature          controllers.TranslatorController.getModelSignature
POST    /getVariablesByGroup        controllers.TranslatorController.getVariablesByGroup
POST    /evaluate                   controllers.TranslatorController.evaluate

# smartAPI

GET     /smart                      controllers.TranslatorController.smart

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
